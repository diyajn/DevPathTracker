package com.org.example.enums;
public enum AutomationLevel {
    MANUAL,      // Full control, lots of code (e.g., JDBC)
    SEMI_AUTO,   // Partial automation (e.g., Hibernate)
    FULLY_AUTO   // Maximum automation (e.g., Spring Data JPA)
}
